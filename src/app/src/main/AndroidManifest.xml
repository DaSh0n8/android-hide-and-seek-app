<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <uses-feature android:name="android.hardware.camera.any" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <application
        android:name=".HideAndSeek"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HideAndSeek"
        tools:targetApi="31">
        <activity
            android:name=".LobbyTutorial"
            android:exported="false" />
        <activity
            android:name=".BasicGameTutorial"
            android:exported="false" />
        <activity
            android:name=".EndGameTutorial"
            android:exported="false" />
        <activity
            android:name=".PlayGameTutorial"
            android:exported="false" />
        <activity
            android:name=".ChooseGameSettingsTutorial"
            android:exported="false" />
        <activity
            android:name=".StartGameTutorial"
            android:exported="false" />
        <activity
            android:name=".TutorialPageMain"
            android:exported="false" />
        <activity
            android:name=".GamePlay"
            android:exported="false"
            android:launchMode="singleTop" />
        <activity
            android:name=".SelfieSegmentation"
            android:exported="false"
            android:launchMode="singleTop" />
        <activity
            android:name=".UserSetting"
            android:exported="false"
            android:launchMode="singleTop" />
        <activity
            android:name=".LobbySettings"
            android:exported="false"
            android:launchMode="singleTop" />
        <activity
            android:name=".JoinGame"
            android:exported="false"
            android:launchMode="singleTop" />
        <activity
            android:name=".GameOver"
            android:exported="false"
            android:launchMode="singleTop" />
        <activity
            android:name=".Lobby"
            android:exported="false"
            android:launchMode="singleTop" />
        <activity
            android:name=".NewGameSettings"
            android:exported="false"
            android:launchMode="singleTop" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDL9-UKCOaEhFR7oPdW_Y4QTIzMpKy-qaQ" />

        <activity
            android:name=".HomeScreen"
            android:exported="true"
            android:label="@string/title_activity_game_play"
            android:launchMode="singleTop">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".HideAndSeek"
            android:exported="true" />
    </application>

</manifest>